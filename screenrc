# from http://www.mattcutts.com/blog/screen-power-tips-screenrc/
startup_message off # no splash screen
defscrollback 30000 # Use a 30000-line scrollback buffer
autodetach on # autodetach on hangup instead of killing screen completely

# stolen from http://dotfiles.org/~chris71mach1/.screenrc
# hardstatus alwayslastline "%{= dR} %-Lw%50>%{= dG}%n%f* %{= dW}%t%{= dR}%+Lw%<"

